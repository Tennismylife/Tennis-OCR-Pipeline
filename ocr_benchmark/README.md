# OCR benchmark

60-page Gallica benchmark used to measure distributed RapidOCR throughput on GitHub-hosted Linux runners.

The matrix workflow runs three shards with four workers each and uploads one-day artifacts containing OCR text, JSON rows, and per-shard metrics.
